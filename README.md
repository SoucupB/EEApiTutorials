## Tutorials

In order to test any of the test bots, you can just ```cd <project>```, set variables in ```Config.txt``` such as ```game_path``` to the absolute path into the game folder and ```dll_path``` to the absolute path of the ```lib``` folder.

After that, just run ```./BuildAndRun.bat```.